import { DragCardDirective } from './drag-card.directive';

describe('DragCardDirective', () => {
  it('should create an instance', () => {
    const directive = new DragCardDirective();
    expect(directive).toBeTruthy();
  });
});
